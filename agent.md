Always consult `repo_map.md` first.

When files/paths/modules/scripts are added, removed, or moved, update `repo_map.md` in the same change.

Follow `AGENTS.md` for scope, cost-control, and implementation guardrails.

Frontend commands must run from `apps/web` (repo root does not contain the web `package.json`).
