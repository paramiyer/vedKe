# apps/web

Image-first karaoke frontend built with React + Vite + TypeScript.

## Commands
- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Routes
- `/` sample slug list
- `/suktas/:slug` karaoke playback with image bbox highlighting
- `/annotate` dev-only bbox + timing annotation tool
